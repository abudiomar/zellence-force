{
  "name": "@zellforce/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run typecheck",
    "typecheck": "bunx tsc -b",
    "test": "vitest run --root ../.. apps/worker --passWithNoTests"
  },
  "dependencies": {
    "@zellforce/config": "workspace:*",
    "@zellforce/domain": "workspace:*"
  }
}
